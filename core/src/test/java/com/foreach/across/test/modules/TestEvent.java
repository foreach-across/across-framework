package com.foreach.across.test.modules;

import com.foreach.across.core.events.AcrossEvent;

public class TestEvent extends AcrossEvent
{
	public TestEvent( Object source ) {
		super( source );
	}
}
