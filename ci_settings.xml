<settings>

	<servers>
		<server>
			<id>across</id>
			<username>${env.FOREACH_NEXUS_USERNAME}</username>
			<password>${env.FOREACH_NEXUS_PASSWORD}</password>
		</server>
		<server>
			<id>sonatype</id>
			<username>${env.SONATYPE_NEXUS_USERNAME}</username>
			<password>${env.SONATYPE_NEXUS_PASSWORD}</password>
		</server>
	</servers>

</settings>
